name: Build JSON from CSV

on:
  push:
    paths:
      - 'data/csv/F1CC_2026.csv'   # Only runs when this CSV is pushed

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write              # Needed to push the updated JSON back

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas

      - name: Run builder
        run: python build.py

      - name: Commit updated JSON files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/races_2026.json data/db.json
          git diff --staged --quiet || git commit -m "Auto: rebuild JSON from CSV [skip ci]"
          git push
